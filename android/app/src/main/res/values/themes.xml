<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.DracPaste" parent="android:Theme.Material.Light.NoActionBar" />

    <!-- Para la activity de captura del portapapeles: sin ventana visible, sin
         animación y sin aparecer en recientes. Se define desde la Fase 0 porque el
         tema es tan crítico como el código: si la activity dibujara algo, el usuario
         vería un parpadeo en cada envío. -->
    <style name="Theme.DracPaste.Invisible" parent="android:Theme.Translucent.NoTitleBar">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowAnimationStyle">@null</item>
        <item name="android:backgroundDimEnabled">false</item>
    </style>
</resources>
